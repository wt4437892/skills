---
name: project-experience-interview
description: 基于当前代码仓库、文档和模块结构提炼可用于项目经历面试的事实证据，生成面试官视角的问题树、追问链路和候选人高质量回答。用于把一个真实项目转成简历项目介绍、项目深挖题、架构题、性能与稳定性题、业务闭环题、个人贡献题，或需要根据仓库内容构建可信答案时使用。
---

# Project Experience Interview

## 概览

先建立“项目事实证据池”，再生成题目和答案。始终把内容分成三类：`已确认事实`、`合理推断`、`需要用户补充`，避免把通用八股硬套成项目经历。

## 工作流

### 1. 建立项目画像

先读高价值入口文件：

- `README.md`
- `pom.xml`
- `AGENTS.md`
- `.code-standards/00-INDEX.md`
- 与目标主题相关的模块目录、配置文件、核心 Service/Controller/Mapper

优先抽取这些信息：

- 项目定位：解决什么业务问题，面向什么场景
- 架构形态：单体、微服务、DDD、事件驱动、搜索、缓存、分布式事务
- 技术栈：语言、框架、基础设施、中间件
- 业务域：商品、订单、支付、营销、用户、搜索等
- 复杂度信号：模块数量、链路长度、状态流转、异步处理、事务一致性、性能场景
- 个人发挥空间：你可能负责的模块、难点、优化点、权衡点

把仓库里的明确证据转成可引用表述。做具体结论时，尽量带上模块名、类名、配置项或文件路径。

先读 `references/project-context.md` 获取项目快照，再回到代码做定点验证。

### 2. 设计题目

围绕同一个主题，按三层深度出题：

1. 基础识别题：判断候选人是否真的做过该项目
2. 深挖追问题：逼出架构、边界、异常流和取舍
3. 压力题：追问故障、性能瓶颈、线上事故、复盘和反思

每道题都给出：

- `出题意图`
- `题目正文`
- `优秀回答应覆盖的要点`
- `可继续追问的方向`
- `面试官评分关注点`

出题时优先采用结构化面试思路，而不是临场自由发挥。尽量把问题分配到固定类别：

- `验证题`：核验候选人是否真的接触过该系统、模块或场景
- `行为题`：要求候选人讲一个真实经历，观察其决策和推进方式
- `情景题`：给一个线上问题或架构权衡，观察其分析与应对
- `技能深挖题`：围绕链路、组件、代码实现和异常处理做技术深挖

同一批输出里，尽量保证这四类问题都有覆盖。

优先覆盖以下主题：

- 项目介绍与业务闭环
- 微服务拆分与领域边界
- 核心调用链路
- 事务一致性与幂等
- 缓存、搜索、消息队列
- 高并发场景
- 安全、权限、风控
- 可观测性、排障、稳定性治理
- 团队协作、推进方式、取舍与复盘

详细题型和问题模板见 `references/interview-playbook.md`。

### 3. 构建答案

用候选人口吻回答，但只基于仓库事实和合理推断。

优先采用这个结构：

`背景 -> 职责 -> 方案 -> 关键细节 -> 取舍 -> 结果 -> 复盘`

如果用户需要“单题标准回答”，优先退化成强化版 STAR：

- `Situation`：只交代必要背景，不堆细节
- `Task`：明确你负责什么、目标是什么
- `Action`：作为回答主体，重点写你亲自做了什么
- `Result`：写结果、指标、收益或至少写观察到的变化

控制讲述重心时，优先把篇幅压在 `Action`。如果答案明显在背景铺陈上过长，主动收缩背景，补充动作、权衡和结果。

写答案时遵守这些约束：

- 优先用第一人称，但不要虚构“我主导了全部模块”
- 没有直接证据时，明确写成“如果你的职责覆盖这里，可以这样回答”
- 能引用代码证据时，写清模块和实现位置
- 能量化再量化；不能量化就写机制和观察指标，不要编数字
- 解释“为什么这样做”，不要只罗列组件名
- 对中高级岗位，必须补充失败场景、边界条件和替代方案

### 4. 贴合目标岗位

根据目标调整题目和答案重心：

- Java 后端：强调服务拆分、RPC、事务、缓存、SQL、排障
- 架构师/高级工程师：强调边界划分、演进路径、治理策略、容量评估
- 面试官视角：强调区分度、追问点、打分标准、真假识别
- 候选人备面：强调可信度、可讲述性、细节密度、前后一致

### 5. 输出固定格式

默认输出以下任一种或组合：

- `项目介绍稿`：1 分钟 / 3 分钟 / 5 分钟版本
- `高频题清单`：按主题分组
- `题目 + 标准答案`：每题带回答框架
- `追问树`：主问题、二级追问、压力追问
- `补信息清单`：哪些个人经历必须由用户补充后才能更可信

如果用户没有指定格式，默认输出：

1. 项目 3 分钟介绍
2. 10 道高频题
3. 每题的示范答案
4. 3 个最可能被继续深挖的风险点

如果用户明确要求“面试官模式”，额外输出一个简易评分表，每题至少包含：

- `识别信号`：什么回答说明候选人真的做过
- `减分信号`：什么回答像背八股或虚构经历
- `建议追问`：下一问如何把回答压实

如果用户明确要求“候选人模式”，额外输出：

- `可直接背诵版`
- `面试口语版`
- `待你补充的信息`

## 真实性约束

始终遵守以下规则：

- 不虚构线上指标、团队规模、职责范围、事故经历
- 不把 README 里的宣传文案直接当成候选人的个人贡献
- 不把“项目支持某能力”直接说成“你亲自完成该能力”
- 不确定时直接标记为 `推断` 或 `待补充`
- 发现 README、代码、模块数量存在不一致时，优先提示并解释口径差异

输出行为题答案时，优先使用 `我` 而不是 `我们`，除非必须解释团队配合。这样可以把个人动作和团队结果拆开，避免贡献模糊。

## 外部校准要点

结合外部高质量面试资料，默认加入这些校准：

- 行为题和项目题都尽量要求“具体例子”，不要接受纯观点回答
- 一组题目里不要只问经历题，要混合验证题、行为题、情景题和深挖题
- 回答是否优秀，不只看技术名词，还看是否讲清楚决策、动作、结果和复盘
- 面试官输出尽量采用固定评分维度，减少主观“感觉不错”
- 候选人输出尽量准备 3 到 5 个可复用项目故事，再映射到不同题目，而不是每题临时编故事

## 参考资料

- `references/project-context.md`
  用于快速掌握当前仓库的业务定位、技术栈、模块分布和高价值面试主题。
- `references/interview-playbook.md`
  用于选择题型、组织追问链路、构建答案结构和识别不可信表述。
- `references/external-benchmarks.md`
  用于查看这次技能迭代参考的外部面试方法论，以及它们应如何落到技能输出中。
